import { Container } from '@pixi/display';
import { Graphics } from '@pixi/graphics';
export type MaskedFrameOptions = {
    target: string | Container;
    mask: string | Graphics;
    borderWidth?: number;
    borderColor?: number;
};
/**
 * Draws a border or apply a mask of any shape to a container.
 * @example
 * ```
 * new MaskedFrame({
 *     target: `avatar.png`,
 *     mask: `avatar_mask.png`,
 *     borderWidth: 5,
 *     borderColor: 0xFFFFFF,
 * });
 * ```
 */
export declare class MaskedFrame extends Graphics {
    /** Target container. */
    target: Container;
    /** Mask to apply. */
    targetMask: Container;
    /** Border color. */
    borderColor: number;
    /** Border width. */
    borderWidth: number;
    constructor({ target, mask, borderWidth, borderColor }: MaskedFrameOptions);
    /** Shows a border. */
    showBorder(): void;
    /** Hides a border. */
    hideBorder(): void;
}
//# sourceMappingURL=MaskedFrame.d.ts.map